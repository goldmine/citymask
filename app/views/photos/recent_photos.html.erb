

<ul id="photos">
	<% if !@photos.blank? %>
	  <% for photo in @photos %>
	    <li>
		  <%= link_to image_tag(photo.public_filename('thumb')), 
		              user_album_photo_path(photo.user, photo.album, photo) %>
		</li>
	  <% end %>
	<% end %>
	
</ul>