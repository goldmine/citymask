<li id="tag-<%= edit_tag.id %>">
  <span><%= edit_tag.name %></span>
  <small>
    [<%= link_to_remote 'delete',
          :url => remove_tag_user_album_photo_path(@user, @album, :id => @photo.id,
                                           :tag_id => edit_tag.id),
          :method => :delete %>]
  </small>
</li>